# AI model files

Put private model directories under this folder.

The default service path is:

```text
accounting_ai_service/models/my_model
```

Large model files are intentionally ignored by Git. Upload them directly to the deployment environment, attach a persistent disk, or build a private Docker image that includes them.

